extend ../template/base.pug
include ../mixins/video

block content
    div.video__grid 
        each info in user.videos
            +video(info)
        else 
            span.empty__message No videos found