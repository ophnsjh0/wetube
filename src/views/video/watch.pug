extends ../template/base.pug

block content 
    video(src="/" + video.videoPath, controls, width="600", height="800")
    div
        p=video.description
        small=video.creatAt
    a(href=`${video.id}/edit`) Edit Video &rarr;  
    br 
    a(href=`${video.id}/delete`) Delete Video &rarr;  